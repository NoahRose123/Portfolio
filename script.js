// ==========================================
// SMOOTH SCROLL (OPTIONAL)
// ==========================================

// Basic smooth scroll is handled by CSS scroll-behavior
// This file can be left minimal or removed if not needed

// Optional: Log to console
console.log('Portfolio loaded');

// Optional: Simple fade-in on load (already handled by CSS)
document.addEventListener('DOMContentLoaded', () => {
    document.body.style.opacity = '1';
});
